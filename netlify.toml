[build]
  command   = "pnpm install && pnpm --filter @workspace/link-hider run build"
  publish   = "artifacts/link-hider/dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
